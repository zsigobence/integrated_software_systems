{
  "name": "Hybrid V2 (Reactive GK + Roles: Presser / Support / Wide)",
  "Kp": 0.35,
  "Kd": 0.45,
  "roles": {
    "fixed": [
      { "x": "50",  "y": "Math.max(390, Math.min(610, by))" },
      { "x": "180", "y": "Math.max(300, Math.min(700, by))" }
    ],
    "dynamic": {
      "sortBy": "distance_to_ball",
      "phases": [
        {
          "condition": "true",
          "targets": [
            { "ax": "rl_ax(0)", "ay": "rl_ay(0)" },
            { "x": "Math.max(300, normBx - 250)", "y": "Math.max(80, Math.min(fieldHeight - 80, by - 120))" },
            { "x": "1800", "y": "fieldHeight / 2" }
          ]
        }
      ]
    }
  }
}
